spring.application.name=${APP_NAME:serviceRegistry}
server.port=${SERVER_PORT:8761}
# Eureka Server
eureka.instance.hostname=${EUREKA_HOST:localhost}
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
